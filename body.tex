% ============================================================
%  body.tex --- the document body. ONE copy, read by every main file.
%
%  There are four main files: two languages times two paper formats. Before
%  this file existed there were two, each carrying its own copy of the wiring
%  below, and one of them was once rewritten with the introduction dropped --
%  every other check passed, because nothing that compares programs can see a
%  difference that lives in the wiring. C15 was written to catch that.
%
%  Four copies would have been four chances to make the same mistake. So the
%  wiring lives here once and is parameterised by \booklang, exactly as
%  structure.tex already parameterises the program sequence. C15 still runs,
%  and now it is checking that the main files agree about *which* body they
%  read rather than about the body itself.
% ============================================================

\begin{document}

% ------------------------------------------------------------
\frontmatter

\input{frontmatter/\booklang/titlepage}

%  The copyright page is the VERSO of the title page, so titlepage.tex ends
%  with \clearpage rather than \cleardoublepage and this file fills the page
%  that \cleardoublepage used to leave blank. Moving it, or turning that
%  \clearpage back into a \cleardoublepage, costs the book a leaf.
\input{frontmatter/\booklang/copyright}

%  There is no preface and no acknowledgements, and that is a decision rather
%  than an omission: the Introduction already does a preface's work -- who the
%  book is for, what it assumes, what it refuses to claim -- and a second
%  chapter saying the same thing before it would be read as throat-clearing.
%  Recorded here because an absence in a directory listing is not a decision.

\tableofcontents

\input{frontmatter/\booklang/how-to-use}
\input{frontmatter/\booklang/introduction}

% ------------------------------------------------------------
\mainmatter

\input{structure}

% ------------------------------------------------------------
%  Appendix A carries the answers. They are collected as the programs are
%  typeset, so this part must come after \input{structure} and nothing may
%  call \printanswers as well -- doing both prints every answer twice.
\appendix
\part{\lblPartAppendices}
\include{appendices/\booklang/appA-answers}
\include{appendices/\booklang/appB-notation}
\include{appendices/\booklang/appC-formulae}
\include{appendices/\booklang/appD-terminology}
\include{appendices/\booklang/appE-further-reading}
\include{appendices/\booklang/appF-manifest}

% ------------------------------------------------------------
\backmatter

\printindex

\end{document}
