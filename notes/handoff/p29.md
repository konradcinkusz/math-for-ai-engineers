# P29 hand-off --- issue #43, worked September 2026

## What the brief promised, and what was already spent

**All of it was already spent, by P29's own drafting pass.** Issue #43 is the
drafting issue and it predates the program: `programs/{en,pl}/P29-entropy.tex`
carries thirty-four teaching frames in both editions with no `\programstub{}`
left in either, and CLAUDE.md records the pass under *Program P29 pass, August
2026*. Nothing in the brief's contract needed writing.

So the pass this issue actually had left was **auditing the delivered program
against its own \enquote{Done when} list**, and one item on that list is not
met by a program that has shipped: *at least one trap frame that elicits the
error before correcting it*. Frame 22's answer box \dash{} and the Quiz Q4 key
reproduced from it \dash{} printed the exact unguarded sentence frame 25 then
calls a misreading, so the program's headline trap was disarmed three frames
before it fired, in the answer box the reader covers and in the key they mark
themselves against. The review issue #138 is the evidence for that and for
twenty-two more findings; the correctness half of it is what this branch fixes.

**Frames: 34, unchanged. Elicitation: 19/34, 55%, unchanged** \dash{} joint
highest outside Part I. No frame was added, removed or converted, so no
`\teachesat`, `\outcome` or `\sumitem` payload moved and the return index is
untouched.

## The two majors

**1. The answer box stated the trap.** Frame 22's `\ans`, its centred italic
statement and the Quiz Q4 key all said *at each position* about a
**corpus-level** perplexity. Frame 25's trapbox then says that exact sentence
is the misreading, so Appendix A carried a Quiz key and a Test key that
contradict each other. All three now carry the guarded reading, and frame 22
gained the sentence that makes the qualifier earn its place rather than look
like hedging: this figure came from a mean loss, so the exponential returns an
average of the per-position counts, and Program F03 names the average it rests
on. Frame 25 now sharpens rather than reverses.

**2. §6 held bits-per-token fixed while changing the tokeniser.** The table
carried one 3.4625 bits/token figure across two tokenisers and called it *the
same model and the same text*, which cannot occur \dash{} a loss per token
belongs to the model **and** its tokeniser together. Bullet 1 and the T5 answer
then gave the reverse account, so the reader was told both that bits per
character is the tokeniser-dependent quantity and that it is the invariant one.

Reframed around what is actually held fixed: **one reported figure**, not one
model. The multiplication, the two bits-per-character figures and the 1.28
ratio are unchanged and still reproduce; what changed is that the section now
says the same number on a model card describes two compressors a factor of 1.28
apart, a `note` says why no single model reports it under both, and bullet 1
and T5 both say plainly that bits per character is the quantity that travels.

## What the script already knew and the page denied

Frame 17's answer said the non-dyadic optimum is *four codewords of two digits
each*. **`best_code` in `code/p29_entropy.py` returns `(1, 2, 3, 3)`** \dash{}
the assignment §3's own \enquote{a length is its own surprise} rule produces
\dash{} because it is the lexicographically first minimiser. Both are Kraft-
complete and both average exactly 2.000, so a reader who followed the previous
frame's method was told they were wrong by the program's own script.

Enumerated and asserted now: `_optimal == [(1,2,3,3), (2,2,2,2)]`, and the frame
names both and makes the tie the evidence \dash{} two quite different codes
cannot both be beaten.

## The transcript did not run

`figures/transcripts/p29-effective-choices.txt` omitted the blank `...` line a
REPL echoes to close a `def`. Replayed through `code.InteractiveConsole` exactly
as printed it raised `SyntaxError: invalid syntax` on the fourth line. Fixed in
the generator and re-replayed: it prints `(4.0, 1.18)`, which is what the page
prints. `make verify` could never have seen it \dash{} the file matched its
generator perfectly.

## Claims about NEIGHBOURS, corrected here and NOT fixed there

Both were confirmed by opening the file rather than reasoned about. Neither
neighbour's file was touched.

- **`programs/en/P19-convexity-jensen.tex:30`** \dash{} the handover to this
  program exists only as a `%%` source comment. P19's printed pages never
  mention P29 or the effective number of choices, so P29 frame 19's *hands one
  thing here by name* was false about what a reader turning to P19 finds. P29
  now says P19 *leaves* the meaning to this program. **A one-sentence forward
  pointer in P19's closing frame or Summary would close it properly.**
- **`programs/en/P19-convexity-jensen.tex:432--441`** \dash{} P19 frame 23
  tabulates the ratio of the wrongly averaged perplexity to the right one (up
  to 7.42 at a spread of two nats) and says nothing about per-token effective
  counts or about orders of magnitude. P29 frame 25 credited it with both. P29
  now says what P19 measured and computes the count consequence as its own
  arithmetic.
- **`programs/en/F03-logarithms.tex:970`** \dash{} F03 prints *What a perplexity
  of 7 says is that at each step the model is as uncertain as a fair seven-sided
  die*. That is the same unguarded *at each step* phrasing P29 frame 25 now
  names as the misreading of a reported perplexity, about the same object (a
  model card's single figure). **Not fixed**: it is another program's file, and
  the F03 review issue is the place for it. P29's own claim that F03 gave the
  perplexity *without saying what the answer was a count of* was false and is
  corrected \dash{} F03 does read it as a die of that many sides; what F03 does
  not do is derive why the exponential of an entropy is a count at all, which
  is what §4 adds.

## Values

Emitted (+5): `p29.eff.upto` 39, `p29.spread.nats` 2.0, `p29.p19.ratio` 7.42,
`p29.count.ratio` 54.6, `p29.att.raw.top` 95.5.
Retired (-1): `p29.eff.checked` 38.

`p29.eff.checked` was the **count** of values swept, and the page read *every
$n$ from $2$ to $\val{p29.eff.checked}+1$* \dash{} a `\val{}` with an arithmetic
operator hanging off it, which reads as the author's own sum left on the page.
The upper limit is emitted instead.

`p29.count.ratio` replaces the attribution to P19 with this program's own
arithmetic: a token a spread above the mean weighs $e^{2\sigma}$ times as many
choices as one a spread below, and the mean cancels, so at P19's spread of two
nats the per-position counts sit a factor of 54.6 apart. **Gated on P19's
committed `p19.ppl.ratio.20`**, whose closed form for near-Gaussian losses is
$e^{\sigma^2/2}$, so the spread this program quotes stops matching if that
measurement ever moves. Guarded with `reproduces()`: the page prints
$e^{2 \times 2.0} = 54.6$ and $e^4$ is 54.598.

`p29.att.raw.top` is P25's own committed `p25.e9.raw.top.512`, read back. The
draft glossed an effective count of 1.12 as *one key and a tenth of another*,
which is a description of a **weight vector** rather than of $e^{H}$ \dash{} a
row at $(1, 0.1)$ normalised has effective count 1.36, not 1.12. The frame now
quotes what P25 measured (95.5 per cent of the weight on one of the eight)
instead of inventing a picture of the row.

**The book's value ledger goes 1673 to 1677**, and `figures/values/appf.tex`
carries it. It was reverted first, per the parallel brief \dash{} and CI's
\enquote{Recompute every number} job then failed on exactly that one line,
because it hard-gates on any `$(COMPUTED)` drift and `make numbers` rewrites
the ledger every run. The brief and that gate contradict each other, so a
branch that emits a value cannot both obey the brief and merge. **Regenerated
on the author's instruction**; PR #201 hit the same wall independently. The
ledger PROSE in CLAUDE.md is still the sync session's and is untouched here.

## Figures

**None moved, none reworded, no new widths measured.** All four were read by
content \dash{} every node, the caption and the manifest copy \dash{} against
the frames on both sides, and rule 2 holds: `surprise-adds` and `two-tokenisers`
close their frame with nothing asked after them; `code-budget` (frame 12) and
`nats-to-count` (frame 20) sit above a question and answer neither, because each
carries the *rule* the frame above states in full while the question below asks
for a number that appears nowhere in it. That is the P04/P07 case \dash{} a
figure the reader is meant to apply belongs above the question.

## Deliberately not done

- **Every page-level finding in #138** \dash{} the Quiz and *Can you?* headings
  stranded above their bodies, the frame-10 and frame-18 trap boxes splitting
  headless, figure 29.2 pushing frame 12's question onto a page alone, frame
  15's stranded tail, the lead-in widow, and the three-quarters-blank closing
  page. This session may not run `make`, so none of them can be measured, and
  the two heading ones are the book-wide room test CLAUDE.md has carried
  unresolved for eight consecutive review batches, whose own diagnosis says the
  instrument is owed before the sweep (`checkpdf`'s heading check learns the
  **numbered** heading's size from the document, so a `\section*` left behind is
  invisible to it).
- **Moving figures 29.2 and 29.3 out of frames 12 and 20.** The reviewer's
  reasoning is partly rhythm and mostly pagination; moving a float re-rolls
  every break after it and the effect cannot be measured from here.
- **Redrawing the four figures**, which are three-box prose chains restating the
  sentence above them. That is the figure pass CLAUDE.md already carries from
  the Foundation batch, and it needs `pdfinfo` widths and a rule-2 re-read in
  all four builds.
- **Editing `F03` or `P19`.** Recorded above instead.

## Gates, all run before anything else

`parity.py` 56 file pairs / 1864 frames / **0 failures, 0 warnings**, clean on
the first run \dash{} no C4, C8, C12 or C16 round.
`check_structure.py --frames --answers --outcomes --values --elicit --scripts
--terms --parts` green; `gen_stubs.py --check` current; `make numbers` exit 0;
`make debt` 1677 computed values all referenced and all present, 88 transcript
references, 0 stubs.
`make verify` reports **only `figures/values/appf.tex`** stale, which is
expected and is the sync session's.

> **Worth knowing for every parallel session, and for the sync one.** The
> Makefile has `verify: numbers`, so *running* `make verify` regenerates every
> values file \dash{} `appf.tex` included \dash{} before it compares anything.
> A session told not to touch that file will therefore find it modified after
> every verify, not once.
>
> **And leaving it reverted makes the branch unmergeable**, which the brief did
> not anticipate. `build.yml`'s numbers job runs `make numbers` and then fails
> on `git diff --name-only -- figures/values figures/transcripts`, so a stale
> ledger is a hard red. Measured here, not inferred: the job log's entire diff
> was the one `appf.values` line. **The brief's rule and the gate cannot both
> be satisfied by a branch that emits a value**, and the sync session should
> settle which gives \dash{} either the brief permits regenerating this one
> generated file, or the numbers job needs to tolerate it on a branch.

The three-instrument prose detector (word joins against `HEAD`, new over-long
source lines, lost paragraph breaks) was run after every scripted substitution
and **watched producing its known answer first** \dash{} it exits 1 with
*no files given -- refusing to report a clean tree* rather than printing
`flagged 0` from a loop that never ran. It caught two over-long lines and one
opaque value key (`percount`, renamed `count`); its remaining flags are ordinary
inflected words for which the line-length instrument reports zero, which is the
recorded two-instrument false-positive pattern.

---

# P29 second reading --- issue #138 verified against current source, September 2026

## Why this section exists, and what it is not

The section above is the **first** P29 session's note, written while it made
the fixes. This one is a **second reader**, working issue #138 directly and
starting from the position CLAUDE.md prescribes: *a review is a claim about
the build it was made on, and this repository moves.* Issue #138 was written
against `main` at `9402620`; the correctness half of it landed in `2aebc24`
and is on `main` now.

So this pass **changed nothing in either edition**. No frame was lengthened or
shortened, no value emitted or retired, no figure moved or reworded, no
payload touched. What it produces is the verification, and one finding in a
neighbouring program that its own review issue closed without fixing.

**The reason to record a verification rather than assume one**: the fixing
session's commit message is itself a claim, and this file's standing rule is
that a fix gets the same scepticism as the defect. Every one of the fourteen
was re-opened against the current file, and the four that carry arithmetic
were recomputed from scratch rather than read off the script that emitted
them.

## The two majors: both retired, on evidence

**M1 --- the answer box stated the trap.** Retired. At `9402620`,
`programs/en/P29-entropy.tex:77` (Quiz Q4 key) and `:523` (frame 22's `\ans`)
both read *At each position*. Both now read *On average*, and the Polish twin
carries the same meaning at `:53` and `:508` (*Średnio*). Frame 25 sharpens
rather than reverses, and the Quiz key and the Test key in Appendix A no
longer contradict each other.

**M2 --- §6 held bits-per-token fixed across two tokenisers.** Retired. The
section now takes *that one reported figure* to two tokenisers rather than
*the same model*; a `note` says a loss per token belongs to the model **and**
its tokeniser together, naming the embedding and output matrices indexed by
the vocabulary, which is the reviewer's own objection made into the teaching
point; *with the weights untouched* is gone; and bullet 1 and the T5 answer
both now say plainly that bits per character is the quantity that travels.
Polish `:753` carries *Weź tę jedną podaną liczbę* and `:931` *tylko bity na
znak*.

## The twelve smaller correctness findings, each re-opened

Fixed and verified: the `\val{}` with an operator hanging off it (the page
now prints `p29.eff.upto` = 39 and `EFF_UPTO = 39` with `range(2, EFF_UPTO+1)`,
so the sweep and the sentence agree); P19's handover softened to *leaves what
the number means to this program*; the F03 claim; the attention table's column
headed *of*; the P19 per-token attribution, now this program's own arithmetic;
the transcript; T5's *only the second*; frame 17's optimal code; frame 10's §6
pointer; the stacked inline radicals; *one key and a tenth of another*; and
further problem 2's spurious qualifier.

**What was recomputed rather than read back:**

- **Frame 17's tie.** Enumerated every Kraft-admissible length assignment over
  $(2/5, 1/5, 1/5, 1/5)$ up to length 7 in exact `Fraction` arithmetic. The
  minimum average is exactly 2, and the minimisers are exactly two:
  `(1,2,3,3)` and `(2,2,2,2)`. The entropy is 1.9219 bits, so the gap is
  0.0781 against a committed `p29.nondy.gap` of 0.078, and
  $\logb{2}\frac{5}{2}$ is 1.3219 against the page's 1.32. The frame's
  *Exactly two, and it is reached two ways* is therefore true and not merely
  plausible.
- **Frame 25's count ratio.** $e^{2\times 2.0} = 54.5982$, which rounds to the
  committed 54.6, and the mean does cancel out of $e^{(\mu+\sigma)}/
  e^{(\mu-\sigma)}$ as the frame claims.
- **§6.** $3.4625 \times 0.25 = 0.866$ and $\times\,0.32 = 1.108$ to the digit
  the page prints, and $1.108/0.866 = 1.28$, so the ratio reproduces from the
  two numbers **as the page prints them** rather than only from the unrounded
  ones.
- **The transcript.** Replayed through `code.InteractiveConsole` line by line,
  feeding the bare `...` continuation as a REPL would --- which is the P32/P33
  lesson about the harness rather than the listing. It prints `(4.0, 1.18)`,
  which is what the page prints.

**And two claims about neighbours were checked in the neighbour's own file**,
because that is the class this book has paid for most often. `F03:950` and
`:963` do name the geometric mean of the per-token probabilities and do say
the perplexity is its reciprocal, so frame 22's new sentence is accurate; and
`F03:971` does read the perplexity as a die of that many sides, so frame 21's
corrected *without deriving why the exponential of an entropy is a count at
all* is the true statement of what F03 leaves undone. `p19.ppl.ratio.20` is
7.42, which is what frame 25 quotes and what the script gates on.

The script carries 34 assertions and reads committed values back from F02,
F03, P19, P25 and F05. `make numbers` exits 0, so every one of those gates
passed against the tree as it stands.

## One thing checked and deliberately left alone

T5's answer ends *a coarser tokeniser raises the loss per token while the
model is unchanged*, and that is the phrasing family the reviewer condemned as
*with the weights untouched* in bullet 1. It looked like the recorded
fixing-an-instance-is-not-fixing-the-class shape, so it was checked rather
than either changed or waved through: `git show 9402620` has the clause
already, the reviewer quoted that answer for a different finding and did not
object to it, and it does not contradict the new note --- the note says no
single model reports *the same figure* under both tokenisers, and T5 says the
figure moves, which is the same statement. **Changing it would have been
inventing a twenty-fourth finding on a judgement call.**

## The nine page-level findings: recorded, with the mechanism

Unchanged from the first session's position, and re-affirmed here because the
mechanism is what the next person needs rather than the list. This session may
not run `make`, so none of them can be measured; and a cue or a tail is one
frame's tail landing a line past a boundary, which is a property of the whole
book and of the installation, so a measurement taken here would be void on the
next merge.

- **The Quiz heading and the *Can you?* heading stranded above their bodies**
  (findings 1 and 13) are one defect and it is the **book-wide room test**
  CLAUDE.md has carried unresolved since the Part V batch, and every review
  batch since has re-recorded rather than taken it. **Its diagnosis was
  sharpened by issue #115 while this pass was running**, and the sharper form
  is worth carrying: `quiz`'s room test of $8\baselineskip$ and the box's own
  `lines before break=12` disagree, so on any page whose remaining room falls
  between the two the reserve **passes**, the heading and its instruction are
  set, and the box then moves whole. In that window the guard does not fail to
  prevent the stranding \dash{} **it produces it**. And *Can you?* is not a
  `\section*` at all, so `checkpdf`'s `heading_metrics`, which learns the
  **numbered** heading's size from the document, cannot match it either.
  #115 corrected those comments and **changed no layout**, so **the instrument
  is still owed before the sweep** --- a pass that takes this needs the checker
  first, then a constant swept against all four builds, which is what the
  `\section` guard's own constant cost when it was chosen.
- **The frame-10 and frame-18 trap boxes splitting headless** (findings 3 and
  5) are the other half of that same class: the Part V batch shipped the
  continuation **mark** for exactly this, so a split box is no longer
  anonymous, and *stopping* the split is the room test above. Those are two
  different jobs whose costs differ by a whole build, which is the distinction
  that batch recorded.
- **Figure 29.2 pushing frame 12's question onto a page alone**, and the same
  construction for 29.3 in frame 20 (finding 4). Moving a `\mermaidfig`
  declaration is a source-level change and it re-rolls every break after it,
  so it cannot be judged from a tree nobody has built.
- **Frame 2's dot row and cue stranded** (finding 2), **frame 15's tail**
  (16), **the lead-in widow** (19) and **the three-quarters-blank closing
  page** (20) are all the editorial random walk across four paginations, and
  the recorded remedy is to **lengthen** rather than trim --- which no
  recorded attempt has yet failed at, and which is the sync session's to walk.

## For the sync session

**Nothing in this pass moves a page**, so there is no cue walk owed on account
of it: no frame changed length in either edition, and `git diff --stat`
against `origin/main` touches this file alone.

**And one finding in a neighbouring program, verified and not fixed, whose own
review issue is closed.** `programs/en/F03-logarithms.tex:970` reads

> *What a perplexity of $7$ says* is that **at each step** the model is as
> uncertain as a fair seven-sided die

and the Polish twin says the same at `programs/pl/F03-logarithms.tex:987`
(*na każdym kroku*). That is the identical unguarded phrasing P29 frame 25
now names as **the misreading**, about the same object --- a model card's
single reported figure --- so the two programs contradict each other on the
book's own headline trap for this material.

It was flagged in F03's own review, **issue #141**, as *'At each step ...'
contradicts the mean-based definition one frame earlier*, with the fix
spelled out. **#141 is closed** (3 September) and the line was not touched:
`git log -S` shows it unchanged since `7fc402b`, and the F01--F03 review batch
`a0c644d` did not reach it. So nobody is coming back to it through that
issue.

Not fixed here because F03 is another unit and another session's file. It is a
two-clause edit in each edition and it needs no measurement, because Summary
item `F03:1334` is already guarded --- it says *A perplexity of 7 is as
uncertain as a fair seven-sided die* with no *at each step* --- so only the
frame is out of step, and the `\result{}` replay into Appendix C is clean.
