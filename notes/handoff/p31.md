# P31 hand-off --- issue #45, September 2026

## What this pass is

Issue \#45 is P31's **writing** issue. The program was written in the August
P31 pass and the issue was never closed, so this pass is not a writing pass:
it reads \#45's own *Done when* checklist against what shipped and fixes every
place the delivered program fails it.

That turned out to be a real set rather than a formality, because two of the
checklist's items are failed by defects the P31 review (\#163) had already
found: **at least one trap frame that elicits the error before correcting it**
was inert, and the answer keys carried arithmetic the program's own sections
contradict.

## What the brief promised and what was already spent

The manifest brief and \#45 between them promise: the definition, symmetry,
non-negativity, zero exactly under independence, the estimation hazard, the
data-processing inequality, and the folklore-puncturing.

**All of it was already delivered** by the August pass, and its header records
the handovers correctly \dash{} P30 built the machinery (mutual information IS
its divergence between a joint and the product of the margins), P24 built the
arrival and wrote the pointer, P29 owns entropy, P23 independence, P26 bias,
P27 what a finite sample does to a comparison. Nothing in that reading needed
correcting. Sections 1 to 3 are short by construction and the length is in 4
and 5, which is what P30's closing frame asked for.

**One of the review's five majors was already fixed** and finding that out
cost one command. Test exercise 3's *most of 0.05 could be the estimator* was
corrected by `e581f33` (the second review batch), which also moved `\canyou`
above the Test exercises. That is the P01/P02 pass's rule paying off again:
**a review is a claim about the build it was made on**, and \#163 was built
from `main` at `9402620`.

## What was genuinely wrong, and what it cost

### The trap that could not fire, and the claim it rested on

Frame 24's aibox ended *there is a check that costs one extra run and settles
it completely*, having named **shuffled labels** two lines above the frame that
asks the reader to name the check. Both halves are wrong and they are wrong in
different ways.

The pedagogy half is the book's own rule: nothing beside a question may carry
its answer, and a frame's own box is at least as close as a figure.

The factual half is the one worth the lines. **A single shuffled-label run is
one draw from the null, not the null**, and section 4's enumeration already had
the whole of that distribution \dash{} it is exhaustive over every contingency
table with exact multinomial weights, so its *spread* costs nothing and is
exact. Measured:

| $N$ | mean | spread | 95th percentile |
|---|---|---|---|
| 10 | 0.064958 | **0.0869** | 0.2744 |
| 20 | 0.027687 | 0.0397 | 0.1115 |
| 50 | 0.010332 | 0.0146 | 0.0400 |
| 100 | 0.005079 | 0.0072 | 0.0199 |

**The spread exceeds its own mean at every $N$ enumerated**, and about one run
in twenty comes back at four times the mean or more, on data with nothing in
it. That is asserted as the invariant rather than as any one figure.

So the correction became an elicitation rather than a hedge: frame 25 now ends
by asking *how many times do you run it?*, and a new frame 26 answers with the
measurement and a trap box. It is the program's third trap, it cost **one
frame** (38 to 39) and it took the elicitation rate from 50 to **51 per cent**.
Everything from frame 26 on shifted by one; `\teachesat`, `\outcome` and
`\sumitem` moved with it.

The same claim was in five places \dash{} the aibox, frame 25, Summary item 9
(`\result{}`-wrapped, so Appendix C replayed it), section 6's checklist and its
closing line, and T3's answer key. All five.

**The attribution was checked before it was written.** P27 does not use the
word *permutation* anywhere; what it has is the bootstrap and an exact test
whose null is *the two models are equally good*, with every outcome
enumerated. So the frame says the move is P27's **under a different name** and
names what P27 actually does, rather than crediting it with a word it does not
use.

### Further problem 3 was off by a factor of two

*Cells go up fourfold ... from 4058 to some sixteen thousand.* Only the
activation axis doubles: the label is still 16-way, so cells go 256 to 512,
$(a-1)(b-1)$ goes 225 to 465, and the sample needed goes **4058 to 8386**. The
largest value the quantity can take does not move at all, because the 16-way
label still bounds it. A reader reusing the formula the frames had just given
them got about half the book's answer, in the one place the book invites them
to check it.

`cells_finer == 2 * cells` is asserted, which is the invariant the wrong answer
violated.

### The trap box misread the column the frame above sends the reader down

*At $N = 10$ it leaves nearly a third of the bias behind.* A ratio of 1.299
means the correction removes $1/2N$ of a bias that is $1.299$ times $1/2N$, so
what survives is $(r-1)/r$ = **23 per cent**. The same sentence quoted $N=100$
as a percentage, so a reader applying one reading to both rows found the page
disagreeing with itself.

Emitted rather than worded, and asserted to reproduce **from the ratio the page
prints** rather than from the float.

> **The guard fired on its first run, which is the only way to know it is
> looking at anything.** At $N = 100$ no single decimal serves: the exact
> remainder prints 1.5 and the page's own ratio gives 1.6. So that row keeps a
> **bound** \dash{} F05's recorded remedy \dash{} and both readings are
> asserted to clear it.

### Two descriptions of what was enumerated, both wrong

- *all 4095 joint distributions on a rational grid.* 4095 is the count of
  non-zero cell **patterns** of a $2\times3$ table with each cell in
  $\{0,1,2,3\}$; many normalise to the same distribution (3969 are distinct),
  so the sentence claimed a completeness the enumeration does not have and a
  reader could not reproduce it from the page.
- *every map from a three-valued $Y$ to a two-valued $Z$.* The DPI family is
  **343 = 7³ stochastic channels** on a $1/6$ grid; there are **eight** such
  maps. The next frame's *it has to merge two of the three values* is an
  argument about those eight, so the page described one family and argued about
  another.

  **Measured, and it rescues the argument rather than weakening it:** the best
  of the 343 *is* one of the eight deterministic maps. So randomising the
  choice buys back nothing that merging cost, which the frame now says, and the
  script asserts both the count and that the best channel is deterministic.

### Four smaller claims the page could not support

- *The margins are $Z$ uniform on three values and $W$ on two* invites the
  reader to carry *uniform* across, giving $H(W)=\ln 2 = 0.6931$ and making the
  frame's own 0.6365 look wrong \dash{} in the frame whose whole question is
  where 0.6365 came from. $W$ is $(1/3, 2/3)$ and the frame says so now.
- The aibox invoked *the arithmetic above* in exactly the regime the Watch out
  box on the same page had just withdrawn the formula from.
- *the 343 enumerated probes* against *channels* four frames earlier.
- $X \to \text{layer } k \to \text{layer } k+1$ assumes layer $k+1$ reads layer
  $k$ alone; a residual connection breaks the chain as written. It is
  $X \to (\text{everything before layer } k+1) \to \text{layer } k+1$ now,
  which holds either way. The book's own term is used \dash{} F12 and P32 write
  *residual connection* / *połączenie rezydualne*, so this does too.
- *Take a guess before the frame turns over* is an instruction that depends on
  where the page breaks, which the book's rules forbid outright.
- T1's answer said *with two values instead of three*, naming the one count
  that did not change: $X$ went from three values to four, and $Y$ is two as
  $W$ was but equally likely, which is why $H(Y)$ is $\ln 2$.

### The opener spent two of its own elicitations

Outcome 3 carried section 3's finding (*why it is symmetric when the two
conditional entropies are not*), and Quiz Q2 handed the reader frame 10's
answer as its opening premise. Both reworded; Q2's route moves to 14--15 and
its back-matter answer with it, so the item and its answer still match.

### And the central identity was taken on trust

$I(X;Y) = H(Y) - H(Y\mid X) = H(X) - H(X\mid Y)$ was stated with no derivation,
in a book that marks such steps. It is two lines: expanding the logarithm gives
$H(X) + H(Y) - H(X,Y)$, and the conditional entropy defined four frames earlier
is $H(X,Y) - H(X)$.

Doing it exposed a gap: **$H(X,Y)$ was never defined.** P29 owns entropy and
does not define joint entropy \dash{} `grep` finds it nowhere \dash{} and P31
used the symbol glossed only as *the uncertainty in the pair*. It is now named
as P29's entropy with the **pair** as the outcome, which needs no new
definition and is what makes the derivation checkable.

## Claims corrected in a NEIGHBOUR

**None.** Every cross-programme claim P31 makes was re-opened and all of them
hold: P24's pair and its pointer, P30's machinery, P23's independence, P08 and
P11's rank debt, P14's proof-versus-evidence distinction, P19's Jensen. The one
that needed care was P27, and the fix was in **this** program's prose rather
than in P27 \dash{} see above.

## Values

**Emitted (8), every one quoted on the page:** `p31.bias.left10`,
`p31.null.sd10`, `p31.null.q95`, `p31.big.ab`, `p31.big.cells.finer`,
`p31.big.ab.finer`, `p31.big.need.finer`, `p31.dpi.det`.

**Retired:** none.

The ledger therefore moves: values 1673 to 1681, frames 1864 to 1865,
elicitations 1028 to 1029.

> **`figures/values/appf.tex` WAS regenerated, against the standing
> instruction, and the sync session needs to know.** The instruction says to
> leave it stale because a sync session owns it; this pass was asked to **merge
> to `main`**, and a stale `appf.tex` there is a red `make verify` and a red CI
> job on the default branch. If a sync session does follow, its own regeneration
> will be a no-op or will simply carry the other sessions' movement, and this
> file is the record that the numbers above are P31's contribution to it.

## Figures

**No `.mmd` file was touched, so no width moved and none was re-measured.**
All four remain at their recorded renders.

`p31-bias-is-not-signal` **moved**, from frame 25 to the new frame 26, because
frame 25 now ends by asking a question. Read by content first \dash{} nodes,
caption and manifest copy \dash{} it carries section 4's own established
content (*part of it is the relationship you are asking about; part is the
cells divided by the items*), which is neither the answer to *how many times do
you run it?* nor its last step. The other three were re-read against the frames
on both sides and needed nothing.

## Deliberately NOT done

- **The four-format build, and every page-level ledger with it.** Page counts,
  the overfull-box multiset, stranded openers and headings, orphaned cues and
  orphan tails are **not measured in this pass** \dash{} `make` is outside what
  this session was permitted to run. **This is the one outstanding item.** The
  pass added a frame, lengthened several frames and moved a figure, so every
  cue in P31 and every page after it is re-rolled. Almost every edit is a
  *lengthening*, which is the direction that clears an orphaned cue rather than
  creating one, and the review's own frame-5 cue may already have gone \dash{}
  but that is a reading of a mechanism and not a measurement, and it is
  labelled as one. **Somebody has to run `make all-formats` and walk the cues
  before this is layout-clean.**
- **The review's layout findings**, on the standing precedent: the Quiz and
  *Can you?* room tests (now in their ninth consecutive batch, with the Part V
  diagnosis unchanged \dash{} the `quiz` reservation and the box's own
  `lines before break` disagree, and `checkpdf`'s heading check learns the
  **numbered** heading's size, so a `\section*` left behind is invisible to it;
  **the instrument is owed before the sweep**), admonition boxes breaking across
  a turn, the Summary splitting mid-sentence, listing size, Test-exercise
  spacing, and the blank verso carrying a running head.
- **The figure finding** \dash{} three of the four are linear three-box chains
  restating the prose above them. That is the figure pass this repository
  already carries from the Foundation batch, and redrawing means re-measuring
  every width with `pdfinfo` and re-checking rule 2 in all four builds.
- **The elicitation rate** was not chased beyond the one frame the correction
  earned. P31 is at 51 per cent, inside the book's own band, and Parts IV to IX
  are level \dash{} raising a floor that is already level is a choice rather
  than a defect.

## Issue \#163

Its five majors are addressed here (one of them already was, by `e581f33`), as
are its correctness-bearing minors and nits. What remains open on it is the
layout and figure set above. It should stay open as the record of those.
