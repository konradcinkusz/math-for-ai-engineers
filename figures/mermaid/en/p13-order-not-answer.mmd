flowchart LR
  A["a directed graph with no cycle admits an order in<br/>which every vertex comes after everything it needs"]
  A --> B["there is usually more than one such order, because<br/>vertices that do not depend on each other may swap"]
  B --> C["and every one of them computes the same values, so a<br/>scheduler may choose whichever of them it likes"]
