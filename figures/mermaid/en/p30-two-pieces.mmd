graph LR
  A["what a code costs:<br/>each codeword length,<br/>weighted by how often<br/>its symbol turns up"] --> B["the floor:<br/>the source's own entropy,<br/>which no whole-digit<br/>code quite reaches"]
  B --> C["what is left over:<br/>a quantity of its own,<br/>and it has a name"]
