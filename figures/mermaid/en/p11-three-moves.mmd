flowchart LR
  A["turn the input space until the directions<br/>the matrix treats separately line up with the axes"]
  A --> B["stretch each of those axes by its own<br/>number, which is the only scaling that happens"]
  B --> C["turn the result again, into whatever space<br/>the output lives in, which may be a different size"]
