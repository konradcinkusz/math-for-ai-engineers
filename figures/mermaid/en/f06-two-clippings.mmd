flowchart LR
  A["one gradient and one<br/>threshold of 1.0, and<br/>two different operations"]
  A --> B["by value truncates each component<br/>on its own, so the vector turns"]
  B --> C["by norm rescales the whole vector,<br/>so the direction survives exactly"]
