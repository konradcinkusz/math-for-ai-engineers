flowchart LR
  A["every optimiser here writes the same line:<br/>subtract a step size times an estimate"]
  A --> B["what changes between them is the estimate,<br/>never the shape of the update"]
  B --> C["so the list is not six algorithms to learn<br/>but one line and a sequence of repairs"]
