flowchart LR
  A["count the operations, count the bytes moved,<br/>and divide the one by the other"]
  A --> B["the device has a ratio of its own: how<br/>many operations per byte it can deliver"]
  B --> C["above that ratio you wait on arithmetic,<br/>and below it you wait on memory"]
