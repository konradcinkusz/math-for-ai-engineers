flowchart LR
  A["forty models whose true<br/>accuracies are exactly equal"]
  A --> B["each measured once, so each<br/>score carries its own error"]
  B --> C["and the table is sorted, which<br/>puts the luckiest error on top"]
