flowchart LR
  A["a derivative of many outputs by many inputs is a table,<br/>and the only question is which way round to write it"]
  A --> B["one convention runs the outputs down the rows, and this<br/>book takes that one and says so rather than assuming it"]
  B --> C["the other runs the inputs down the rows instead, so a<br/>derivation has to pick one of them and hold it"]
