>>> from p05_inner_product_norms import unit, dot
>>> import random, statistics          # run this from code/
>>> def spread(d, n=4000):
...     rnd = random.Random(20260831 + d)
...     return statistics.pstdev(
...         dot(unit(d, rnd), unit(d, rnd)) for _ in range(n))
...
>>> print(*(f"{spread(d):.4f}" for d in (2, 10, 100, 768)))
0.7054 0.3180 0.1001 0.0356
