%% P13 --- Logika, dowód i czytanie twierdzeń
%%
%% Zalążek. Poniższy opis jest kontraktem tego programu: co ma uzasadnić i co
%% ma dać czytelnikowi. Napisanie programu oznacza usunięcie bloku
%% \programstub{} i zastąpienie go programem. Jeśli po przerobieniu materiału
%% opis okaże się błędny, popraw go i odnotuj sprzeczność w CLAUDE.md w sekcji
%% *Resolved questions*.

\program{Logika, dowód i czytanie twierdzeń}
\label{prog:P13}

\programstub{%
\textit{[Opis do przetłumaczenia --- patrz wydanie angielskie.]}

Argument: implication is not equivalence, a quantifier's order changes the
claim, and a proof by induction or contradiction is a shape you can
recognise. Payoff: the reader can read a paper's theorem and separate its
hypotheses from its conclusion. Worked on real examples of the gap: a
convergence result that assumes convexity being cited about a neural
network; a bound that holds \enquote{with high probability over the draw of the data\enquote{ quoted as if it held for the dataset in hand; }universal approximation} quoted as if it said anything about learnability. This
program is the honest, bounded fix for Stroud's absent rigour: it does not
train the reader to *write* proofs, and says so.

\medskip
\textbf{Planowana długość:} 45 ramek.
}
