%% P09 --- Wyznacznik, macierz odwrotna i zmiana bazy
%%
%% Zalążek, wygenerowany przez tools/gen_stubs.py z tools/programs.json.
%% Poniższy opis jest kontraktem tego programu: co ma uzasadnić i co ma dać
%% czytelnikowi. Napisanie programu oznacza usunięcie bloku \programstub{} i
%% zastąpienie go programem -- po czym ten plik nie jest już generowany.

\program{Wyznacznik, macierz odwrotna i zmiana bazy}
\label{prog:P09}

\programstub{%
\textit{[Opis do przetłumaczenia --- patrz wydanie angielskie.]}

Argument: the determinant is a signed volume scale factor and zero means
information was destroyed; an inverse exists exactly when nothing was
destroyed; a change of basis is the same vector described in another
coordinate system. Payoff: why production code almost never calls
\code{inv()} and solves a factorised system instead \dash{} stated here,
measured in P10; a rotation as an orthogonal change of basis, which is what
rotary position embedding does; the log-determinant term in a normalising
flow, named so the reader recognises it later without the book promising to
teach flows.

\medskip
\textbf{Planowana długość:} 45 ramek.
}
