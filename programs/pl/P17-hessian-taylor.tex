%% P17 --- Hesjan, krzywizna i rozwinięcie Taylora
%%
%% Zalążek, wygenerowany przez tools/gen_stubs.py z tools/programs.json.
%% Poniższy opis jest kontraktem tego programu: co ma uzasadnić i co ma dać
%% czytelnikowi. Napisanie programu oznacza usunięcie bloku \programstub{} i
%% zastąpienie go programem -- po czym ten plik nie jest już generowany.

\program{Hesjan, krzywizna i rozwinięcie Taylora}
\label{prog:P17}

\programstub{%
\textit{[Opis do przetłumaczenia --- patrz wydanie angielskie.]}

Argument: the second-order Taylor expansion is the best local quadratic
model, and its matrix is the Hessian. Payoff: **why the largest stable
learning rate is bounded by the inverse of the curvature**, derived on a
quadratic, which turns \enquote{the loss exploded} into an arithmetic
statement; the condition number of the Hessian as the ratio of fastest to
slowest direction, joining P9 and P10 to the optimiser; second-order methods
explained and then dismissed on cost for this problem size; and an honest
section on sharp-versus-flat minima \dash{} what is measured, what is
reparameterisation-dependent, and why the claim is contested.

\medskip
\textbf{Planowana długość:} 50 ramek.
}
