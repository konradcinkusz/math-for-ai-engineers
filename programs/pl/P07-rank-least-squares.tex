%% P07 --- Rząd, podprzestrzenie i najmniejsze kwadraty
%%
%% Zalążek. Poniższy opis jest kontraktem tego programu: co ma uzasadnić i co
%% ma dać czytelnikowi. Napisanie programu oznacza usunięcie bloku
%% \programstub{} i zastąpienie go programem. Jeśli po przerobieniu materiału
%% opis okaże się błędny, popraw go i odnotuj sprzeczność w CLAUDE.md w sekcji
%% *Resolved questions*.

\program{Rząd, podprzestrzenie i najmniejsze kwadraty}
\label{prog:P07}

\programstub{%
\textit{[Opis do przetłumaczenia --- patrz wydanie angielskie.]}

Argument: a matrix has a column space, a row space and two null spaces; rank
is the dimension shared by the first two and it is the honest measure of how
much a matrix does. Payoff: LoRA is a rank constraint, and this is the
program that makes that sentence carry information --- a $d \times d$ update
replaced by $BA$ with inner dimension \code{r}, parameter count
$2dr$ against $d^{2}$, and the assumption being made about the
update stated plainly. Solving $Ax = b$ when there is no solution:
least squares as a projection, which is linear regression's closed form.
Rank collapse in deep attention stacks, described as the phenomenon it is.

\medskip
\textbf{Planowana długość:} 60 ramek.
}
