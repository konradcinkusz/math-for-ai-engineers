%% P07 \dash{} Rank, the four subspaces and least squares
%%
%% Stub. The brief below is the contract for this program: what it must argue,
%% and what it must buy the reader. Writing the program means deleting the
%% \programstub{} block and replacing it with the program. If the brief turns
%% out to be wrong once the mathematics has been worked, change it and record
%% the contradiction in CLAUDE.md under *Resolved questions*.

\program{Rank, the four subspaces and least squares}
\label{prog:P07}

\programstub{%
Argument: a matrix has a column space, a row space and two null spaces; rank
is the dimension shared by the first two and it is the honest measure of how
much a matrix does. Payoff: LoRA is a rank constraint, and this is the
program that makes that sentence carry information \dash{} a $d \times d$ update
replaced by $BA$ with inner dimension \code{r}, parameter count
$2dr$ against $d^{2}$, and the assumption being made about the
update stated plainly. Solving $Ax = b$ when there is no solution:
least squares as a projection, which is linear regression's closed form.
Rank collapse in deep attention stacks, described as the phenomenon it is.

\medskip
\textbf{Planned length:} 60 frames.
}
