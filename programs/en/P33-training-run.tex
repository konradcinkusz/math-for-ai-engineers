%% P33 --- Anatomy of a training run
%%
%% Stub, generated by tools/gen_stubs.py from tools/programs.json.
%% The brief below is the contract for this program: what it must argue, and
%% what it must buy the reader. Writing the program means deleting the
%% \programstub{} block and replacing it with the program -- after which this
%% file is never regenerated. If the brief turns out to be wrong once the
%% mathematics has been worked, change it in the manifest and record the
%% contradiction in CLAUDE.md under *Resolved questions*.

\program{Anatomy of a training run}
\label{prog:P33}

\programstub{%
The loss as maximum likelihood [P26]; its gradient as \code{p - y} [P18];
the optimiser and its bias correction [P20]; precision and loss scaling
[P1]; the schedule and the curvature bound [P17]; gradient clipping and
minibatch noise [P21]. Then the diagnostic half: **a loss curve read as
evidence** \dash{} what a plateau, a spike, a divergence and a suspiciously
smooth descent each imply, and which of them are distinguishable from noise
[P27]. Scaling laws presented as **an empirical power-law fit with reported
uncertainty**, plotted on log-log axes [F3], with the fit's extrapolation
error stated \dash{} not as a law of nature.

\medskip
\textbf{Planned length:} 60 frames.
}
