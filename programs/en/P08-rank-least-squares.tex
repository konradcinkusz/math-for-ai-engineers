%% P08 --- Rank, the four subspaces and least squares
%%
%% Stub, generated by tools/gen_stubs.py from tools/programs.json.
%% The brief below is the contract for this program: what it must argue, and
%% what it must buy the reader. Writing the program means deleting the
%% \programstub{} block and replacing it with the program -- after which this
%% file is never regenerated. If the brief turns out to be wrong once the
%% mathematics has been worked, change it in the manifest and record the
%% contradiction in CLAUDE.md under *Resolved questions*.

\program{Rank, the four subspaces and least squares}
\label{prog:P08}

\programstub{%
Argument: a matrix has a column space, a row space and two null spaces; rank
is the dimension shared by the first two and it is the honest measure of how
much a matrix does. Payoff: **LoRA is a rank constraint**, and this is the
program that makes that sentence carry information \dash{} a \code{d x d}
update replaced by \code{B A} with inner dimension \code{r}, parameter count
\code{2dr} against $d^{2}$, and the assumption being made about the update
stated plainly. Solving \code{Ax = b} when there is no solution: least
squares as a projection, which is linear regression's closed form. Rank
collapse in deep attention stacks, described as the phenomenon it is.

\medskip
\textbf{Planned length:} 60 frames.
}
